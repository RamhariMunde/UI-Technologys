// Splice is use to cut the array element 

const arr = [ 'a','b','c','d'];

arr.splice(1,1,'x');   // in this row i have first one minning is chuse the array element number 
                    // and second number one is cut elements in the  array and third is a replese and add this element 

console.log(arr); // mining is print this array  